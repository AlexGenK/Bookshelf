<table class="table">
	<thead>
		<tr>
			<th>Username</th>
			<th>Roles</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
	<% @users.each do |user| %>
		<tr>
			<td><%= user.username %></td>
			<td>
			<%= form_for user do |f| %>
				<%= f.check_box :superadmin_role %>
				<%= f.check_box :admin_books_role %>
				<%= f.check_box :admin_categories_role %>
				<%= f.submit 'Update roles' %>
			<% end %>
			</td>
			<td><%= link_to 'DEL', user, method: :delete %></td>
		</tr>
	<% end %>
	</tbody>
</table>
